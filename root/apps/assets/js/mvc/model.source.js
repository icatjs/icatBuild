/** {%=appName%}-model.js # */
(function(iCat){
	// Class
	{%=appName%}.Model = iCat.Model.extend({
		// model's common config & property & function
	});

	// setting
	{%=appName%}.Model.setting = {
		// model's settings...
	};
})(ICAT);