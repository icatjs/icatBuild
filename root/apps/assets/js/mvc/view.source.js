/** {%=appName%}-view.js # */
(function(iCat){
	// templates
	{%=appName%}.template = {
		// view's templates...
	};

	// Class
	{%=appName%}.View = iCat.View.extend({
		// view's common config & property & function
	});

	// setting
	{%=appName%}.View.setting = {
		// view's settings...
	};
})(ICAT);